apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: dirkshop-dev-carts-db
spec:
  hosts:
  - "carts-db.dev.35.224.249.133.xip.io"
  gateways:
  - dirkshop-dev-gateway
  http:
    - route:
      - destination:
          host: carts-db.dev.svc.cluster.local
